<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Preparing your chart…</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
    body {
      font-family: system-ui, -apple-system, Segoe UI, Roboto, sans-serif;
      padding: 28px;
      line-height: 1.5;
      color: #222;
    }
    .btn {
      display: inline-block;
      padding: 10px 16px;
      border-radius: 8px;
      text-decoration: none;
      border: 1px solid #ccc;
      background: #f5f5f5;
    }
  </style>
</head>
<body>
  <h1>Your chart is being prepared…</h1>
  <p>If the download doesn’t start automatically, tap the button below.</p>

  <p>
    <a id="dl" class="btn"
       href="<%= chart_pdf_prawn_path(name: @name, date: @date, time: @time, place: @place) %>">
      Download your PDF
    </a>
  </p>

  <p><small>Tip: On iPhone choose “Save to Files.” On Android check your Downloads folder.</small></p>

  <script>
    window.addEventListener('load', function () {
      setTimeout(function () {
        var a = document.getElementById('dl');
        if (a) a.click();
      }, 250);
    });
  </script>
</body>
</html>
