<!DOCTYPE html>
<html>
<head>
 <!-- LAYOUT: EMBED -->
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <%= csrf_meta_tags %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

  <%= javascript_importmap_tags %>

  <!-- Keep the iframe background neutral; all form styling lives in shared CSS -->
  <style nonce="<%= content_security_policy_nonce %>">

    html, body { margin:0; padding:0; background:transparent; }
    /* Optional: a neutral wrapper that doesn't impose layout */
    .embed-container { width:100%; background:transparent; }
  </style>
</head>
<body>
  <div class="embed-container">
    <%= yield %>
  </div>

  <!-- Auto-resize the iframe (no styling here, just behavior) -->
  <script>
    function sendHeight(){
      var h = document.documentElement.scrollHeight;
      parent.postMessage({ hdEmbedHeight: h }, "*");
    }
    new ResizeObserver(sendHeight).observe(document.body);
    window.addEventListener("load", sendHeight);
  </script>
</body>
</html>
